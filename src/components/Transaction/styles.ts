import styled from 'styled-components'

const Wrapper = styled.li``

const Info = styled.div``

const Heading = styled.div`
  margin-left: 1rem;

  h4 {
    font-size: 0.9rem;
    font-weight: normal;
    margin-top: 0.1rem;
    color: var(--color-neutral-700);
  }
`

const Value = styled.h3``

export { Wrapper, Info, Heading, Value }
